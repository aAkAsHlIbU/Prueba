name: preuba
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.5.0
  flutter_svg: ^2.0.10+1
  image_picker: ^1.1.2
  firebase_auth: ^5.3.0
  flutter_form_builder: ^9.4.1
  cloud_firestore: ^5.4.2
  google_sign_in: ^6.2.1
  crypto: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true  # Ensures material icons are available
  assets:
    - assets/google_icon.png   
    - assets/images/glitter.jpeg
